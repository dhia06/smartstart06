{% extends 'base.html.twig' %}

{% block body %}
    <div>
        {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
            {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} |
            <button type="button" class="btn btn-danger"><a href="{{ path('fos_user_security_logout') }}">
                    {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                </a></button>
        {% else %}
            <button class="btn btn-info"><a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a></button>
        {% endif %}
    </div>
    <h1  style="color: #9F0053">Reclamation</h1>

    <table border="1">
        <tbody>
            <tr>
                <th>Id11</th>
                <td>{{ reclamation.id11 }}</td>
            </tr>
            <tr>
                <th>Sujet11</th>
                <td>{{ reclamation.sujet11 }}</td>
            </tr>
            <tr>
                <th>Text11</th>
                <td>{{ reclamation.text11 }}</td>
            </tr>
            <tr>
                <th>Phone11</th>
                <td>{{ reclamation.phone11 }}</td>
            </tr>
        </tbody>
    </table>


            <button class="btn btn-warning"><a href="{{ path('reclamation_index') }}">Back to the list</a></button>

    <button class="btn btn-success"><a href="{{ path('reclamation_edit', { 'id11': reclamation.id11 }) }}">Edit</a></button>

            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}

{% endblock %}
