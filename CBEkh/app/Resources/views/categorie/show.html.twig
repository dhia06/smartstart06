{% extends 'base.html.twig' %}

{% block body %}

    <h1 style="color: #9f191f"><b><i><center>Categorie</center></i></b></h1>


    <table class="table table-bordered table-striped" cellpadding="0" border="2">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie.libelle }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorie.description }}</td>
            </tr>
        </tbody>
    </table>

    <div><ul> <button class="btn btn-danger">
                <a href="{{ path('categorie_index') }}" style="color: #0c0c0c">Annuler</a>
            </button></ul></div>
       <div><ul> <button class="btn btn-danger">
                   <a href="{{ path('categorie_edit', { 'id': categorie.id }) }}" style="color: #0c0c0c">modifier</a>
               </button></ul></div>

       <div><ul>
               {{ form_start(delete_form) }}
               <input type="submit" value="supprimer" style="background-color: #9f191f">
               {{ form_end(delete_form) }}

           </ul></div>


{% endblock %}
